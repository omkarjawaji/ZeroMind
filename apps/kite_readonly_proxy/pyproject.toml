[project]
name = "kite-readonly-proxy"
version = "0.1.0"
description = "Allowlist proxy exposing only read tools of Kite MCP (ADR-0002)."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/kite_readonly_proxy"]
