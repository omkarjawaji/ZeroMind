[project]
name = "papertrade"
version = "0.1.0"
description = "Standalone paper/strategy lab. Imports nothing from ZeroMind (ADR-0003)."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/papertrade"]
